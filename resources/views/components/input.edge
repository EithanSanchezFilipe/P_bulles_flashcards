<div class="form-group">
  <label for="{{ name }}">{{ label }}</label>
  @inputError(name)
    @each(message in $messages)
      <p class="input-error">
        {{ message }}
      </p>
    @end
  @end
  <input
    type="{{ type || 'text' }}"
    name="{{ name }}"
    id="{{ name }}"
    value="{{ value || '' }}"
    class="{{ cls || 'form-control' }}"
  />
</div>
